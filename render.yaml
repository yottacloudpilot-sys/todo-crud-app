services:
  - type: web
    name: todo-crud-api
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_PATH
        value: ./todos.db
      - key: CORS_ORIGIN
        sync: false
